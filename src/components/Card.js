import React from 'react';
import ButtonCounter from "./ButtonCounter";

const Card = () => {
    return (
        <div>
            <ButtonCounter/>
        </div>
    );
}

export default Card;